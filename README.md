# FitSense
Python web app that recommends personalized fitness and meal plans from a CSV dataset using NLP similarity search. Plan descriptions are embedded in a vector database, and user queries are matched semantically. Users can filter by gender, fitness goal, and diet. No model training is required; results are deterministic and stateless.
